# Docs: https://github.com/fmind/dot#secret-management
# Retire the legacy exported secrets when upgrading an existing installation.
# Credentials now use native stores or explicit `dot secret run NAME -- command`.
# Restart existing terminals and agent sessions to discard inherited exports.
