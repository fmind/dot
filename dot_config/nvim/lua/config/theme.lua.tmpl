-- The colorscheme name, from theme_variant in .chezmoi.toml.tmpl.
-- The colorscheme file itself is fetched from github.com/fmind/theme by
-- .chezmoiexternal.toml.tmpl into colors/, where vim.g.colors_name matches this.
return "{{ .theme_variant }}"
