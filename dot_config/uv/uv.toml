# Docs: https://docs.astral.sh/uv/reference/settings/

# mise owns the workstation interpreter; prefer it over uv-managed copies so pipx tool
# environments match every project venv.
python-preference = "system"
