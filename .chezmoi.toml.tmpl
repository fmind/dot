encryption = "age"

[data]
    git_name = {{ promptStringOnce . "git_name" "What is your Git name" "Médéric Hurier (Fmind)" | quote }}
    git_email = {{ promptStringOnce . "git_email" "What is your Git email address" "github@fmind.dev" | quote }}
    github_user = {{ promptStringOnce . "github_user" "What is your GitHub username" "fmind" | quote }}
    # Local checkout takes precedence over the pinned release when configured.
    theme_variant = {{ promptStringOnce . "theme_variant" "Which fmind/theme variant" "fmind" | quote }}
    theme_ref = "v2.2.0"
    theme_local_path = {{ promptString "Local theme checkout (empty uses theme_ref)" (get . "theme_local_path" | default "") | quote }}

[age]
    identity = "{{ .chezmoi.homeDir }}/.config/chezmoi/key.txt"
    recipient = "age1uvmlyuvm070d22xtmsadg9a0ucw49fyn4fx0fkd805lp3kd6zy0q745paz"

[cd]
    command = "fish"

[edit]
    apply = true
    command = "nvim"

[diff]
    pager = "delta --paging=never"

[merge]
    command = "nvim"
    args = [
        "-d",
        "{{`{{ .Destination }}`}}",
        "{{`{{ .Source }}`}}",
        "{{`{{ .Target }}`}}"
    ]
