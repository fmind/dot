# Docs: https://docs.github.com/en/code-security/dependabot/working-with-dependabot/dependabot-options-reference
version: 2
updates:
  - package-ecosystem: github-actions
    # "/" covers .github/workflows; any other directory is scanned for *.yml files,
    # which keeps the SHA-pinned workflow templates shipped in skills current.
    directories:
      - /
      - /skills/cloud-run/templates
      - /skills/github-actions/references/ci-cd/templates
    schedule:
      interval: weekly
      day: monday
    cooldown:
      default-days: 7
    commit-message:
      prefix: "chore(deps)"
    groups:
      actions:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
  - package-ecosystem: uv
    directory: /dot
    schedule:
      interval: weekly
      day: monday
    cooldown:
      default-days: 7
    commit-message:
      prefix: "chore(deps)"
    groups:
      python:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
